name: multi_job_pipes

on:
    push:

jobs:
    backend:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - run: echo "Testing backend"

    frontend:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - run: echo "Testing frontend" 
