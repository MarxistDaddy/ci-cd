name: Fix Colons
on: push

jobs:
    build:
        runs-on: ubuntu-ltest
        steps:
            - name: checkout
              
              uses: actions/checkout@v4

            - run: echo Hello
